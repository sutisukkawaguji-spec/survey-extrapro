// Copy to config.local.js for local development only. config.local.js is gitignored.
// Load or merge this file manually while developing; never commit real secrets.
window.SURVEY_LOCAL_CONFIG = {
    googleMapsBrowserKey: 'YOUR_RESTRICTED_DEVELOPMENT_BROWSER_KEY',
    devBypassAuth: true
};
